#include "main.h"

/**
 * main - Tests the function positive_or_negative() with an input of 0.
 * Return: 0 if successful
 */
int main(void)
{
	int i;

	i = 0; 
	/* Testing the function with 0 */
	positive_or_negative(i);

	return (0);
}
